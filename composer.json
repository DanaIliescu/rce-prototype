{
    "scripts": {
        "compile": [
            "chmod -R 777 wp-admin/",
            "chmod -R 777 wp-content/",
            "chmod -R 777 wp-includes/",
            "chmod -R 777 media/"
        ]
    }
}